SELECT * 
FROM PRODUTOS;

SELECT NOMEDOPRODUTO AS PRODUTO, 
       PRECOUNITARIO AS PREÇO 
FROM PRODUTOS;


SELECT NOMEDOPRODUTO AS PRODUTO, 
       PRECOUNITARIO AS PREÇO,
       PRECOUNITARIO*1.25 AS "PREÇO PLAYBOY"
FROM PRODUTOS;

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE PRECOUNITARIO > 60;

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE PRECOUNITARIO >= 60 AND PRECOUNITARIO <= 100;

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE PRECOUNITARIO BETWEEN 60 AND 100;

SELECT 'Prezado(a) Sr(a) ' || NOME || ' ' || SOBRENOME || 
       ' está devendo a empresa'
FROM EMPREGADOS;

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) = 'CHAI';

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE 'CHA%';

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '%A';

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '%OV%';

SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '__A%';


SELECT NOMEDOPRODUTO, PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '%FANTÁSTICA%';


SELECT SUBSTR(NOMEDOPRODUTO,9,10), PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '%FANTÁSTICA%';


SELECT SUBSTR(NOMEDOPRODUTO,9,10), PRECOUNITARIO
FROM PRODUTOS
WHERE UPPER(NOMEDOPRODUTO) LIKE '%FANTÁSTICA%';


SELECT COUNT(*), MAX(PRECOUNITARIO),MIN(PRECOUNITARIO) ,
       ROUND(AVG(PRECOUNITARIO),2), 
       SUM(UNIDADESEMESTOQUE*PRECOUNITARIO)
FROM PRODUTOS    ; 


SELECT *
FROM EMPREGADOS
ORDER BY CARGO;


SELECT *
FROM EMPREGADOS
ORDER BY CARGO, TRATAMENTO;


SELECT DISTINCT CARGO
FROM EMPREGADOS;


SELECT CARGO, COUNT(*)
FROM EMPREGADOS 
GROUP BY CARGO;

SELECT *
FROM DETALHES_PEDIDO;

SELECT NUMERODOPEDIDO, COUNT(*) PRODUTOS , 
       SUM(QUANTIDADE) UNIDADES, 
       SUM(QUANTIDADE*(PRECOUNITARIO*(1-DESCONTO))) VALOR
FROM DETALHES_PEDIDO 
GROUP BY NUMERODOPEDIDO;

SELECT NUMERODOPEDIDO, 
       PRECOUNITARIO NORMAL,
       DESCONTO,
       PRECOUNITARIO*DESCONTO "VALOR DESCONTO",
       PRECOUNITARIO*(1-DESCONTO) "PRECO MENOS DESCONTO"
FROM DETALHES_PEDIDO;