CREATE TABLE DEPARTAMENTO
(
ID_DEPT INT PRIMARY KEY,
NOME VARCHAR(50) NOT NULL,
DESCRICAO VARCHAR(250)
);

CREATE TABLE FUNCIONARIO
(
MATRICULA INT PRIMARY KEY,
NOME VARCHAR(60) NOT NULL,
CPF VARCHAR(11) UNIQUE,
SEXO CHAR(1) CHECK(SEXO IN ('M','F')),
COD_DEPT INT REFERENCES DEPARTAMENTO(ID_DEPT)
)

INSERT INTO DEPARTAMENTO(ID_DEPT,NOME,DESCRICAO)
VALUES (1,'RECURSOS HUMANOS','CUIDA DE GENTE');

INSERT INTO DEPARTAMENTO(ID_DEPT,NOME,DESCRICAO)
VALUES (2,'TECNOLOGIA','DESTROI A GENTE');



INSERT INTO FUNCIONARIO(MATRICULA,NOME,CPF,SEXO,COD_DEPT)
VALUES (1000, 'ANDREZZA','','F',2);

INSERT INTO FUNCIONARIO(MATRICULA,NOME,CPF,SEXO,COD_DEPT)
VALUES (1001, 'PAULO','','M',2);

UPDATE FUNCIONARIO
SET CPF = '258963258'
WHERE MATRICULA = 1001;

----------------------------------------------

ALTER TABLE FUNCIONARIO
ADD SALARIO NUMBER(5,2) DEFAULT 0;

ALTER TABLE FUNCIONARIO
RENAME COLUMN SEXO TO GENERO;

ALTER TABLE FUNCIONARIO
DROP CONSTRAINT SYS_C004052;

ALTER TABLE FUNCIONARIO
MODIFY GENERO CHAR(1) CHECK(GENERO IN ('F','M','N'));

RENAME FUNCIONARIO TO EMPREGADO;